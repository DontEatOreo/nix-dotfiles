[General]
# Keep Bluetooth HID devices owned by the controller while they are connected.
# Some keyboards otherwise recover poorly after short link stalls and can leave a
# key logically held until another input device changes state.
IdleTimeout=0
