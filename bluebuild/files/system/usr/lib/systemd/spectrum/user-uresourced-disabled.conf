[Unit]
# Fedora 44's user uresourced daemon can wedge transient app launches.
# Keep the system daemon and explicit resource policy while leaving the user
# daemon opt-in for regression testing.
ConditionPathExists=/run/spectrum/enable-user-uresourced
