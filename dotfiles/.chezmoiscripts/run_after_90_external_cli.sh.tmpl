#!/usr/bin/env bash
# shellcheck shell=bash
set -euo pipefail

readonly installer={{ joinPath .chezmoi.homeDir ".local/libexec/external-cli-installer" | quote }}
if [[ ! -x $installer ]]; then
	printf 'encrypted external installer is missing: %s\n' "$installer" >&2
	exit 1
fi
"$installer"
