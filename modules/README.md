# Nix modules

This directory contains our Nix modules. The cross-platform modules hold shared Nix
configuration, and the NixOS modules hold reusable NixOS configuration

Machine-specific configuration remains in `hosts/`. Package implementations remain in
`packages/`
