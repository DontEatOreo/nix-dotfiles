---
- name: Detect GNOME support for Hyper window tiling
  ansible.builtin.command:
    argv:
      - gnome-extensions
      - version
  changed_when: false
  failed_when: false
  check_mode: false
  register: desktop_hyper_gnome_command
  tags:
    - host
    - desktop
    - hyper-window-tiling

- name: Detect KDE support for Hyper window tiling
  ansible.builtin.command:
    argv:
      - plasmashell
      - --version
  changed_when: false
  failed_when: false
  check_mode: false
  register: desktop_hyper_kde_command
  tags:
    - host
    - desktop
    - hyper-window-tiling

- name: Check KDE installation paths
  ansible.builtin.stat:
    path: "{{ item }}"
  loop:
    - /usr/share/plasma
    - /usr/share/wayland-sessions/plasma.desktop
  register: desktop_hyper_kde_paths
  tags:
    - host
    - desktop
    - hyper-window-tiling

- name: Select available Hyper window tiling desktops
  ansible.builtin.set_fact:
    desktop_hyper_has_gnome: "{{ desktop_hyper_gnome_command.rc == 0 }}"
    desktop_hyper_has_kde: >-
      {{
        desktop_hyper_kde_command.rc == 0
        or desktop_hyper_kde_paths.results
          | selectattr('stat.exists')
          | list
          | length > 0
      }}
  tags:
    - host
    - desktop
    - hyper-window-tiling

- name: Install Hyper window tiling build dependencies
  ansible.builtin.command:
    argv:
      - bun
      - install
      - --frozen-lockfile
    chdir: "{{ desktop_hyper_window_tiling_package }}"
  changed_when: false
  when: desktop_hyper_has_gnome or desktop_hyper_has_kde
  tags:
    - host
    - desktop
    - hyper-window-tiling

- name: Build Hyper window tiling integrations
  ansible.builtin.command:
    argv:
      - bun
      - run
      - build
    chdir: "{{ desktop_hyper_window_tiling_package }}"
  changed_when: false
  when: desktop_hyper_has_gnome or desktop_hyper_has_kde
  tags:
    - host
    - desktop
    - hyper-window-tiling

- name: Create Hyper GNOME extension directories
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: "0755"
  loop:
    - "{{ desktop_hyper_window_tiling_gnome_dir }}"
    - "{{ desktop_hyper_window_tiling_gnome_dir }}/schemas"
  when: desktop_hyper_has_gnome
  tags:
    - host
    - desktop
    - hyper-window-tiling

- name: Install Hyper GNOME extension files
  ansible.builtin.copy:
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
    mode: "0644"
  loop:
    - src: "{{ desktop_hyper_window_tiling_package }}/gnome/metadata.json"
      dest: "{{ desktop_hyper_window_tiling_gnome_dir }}/metadata.json"
    - src: "{{ desktop_hyper_window_tiling_package }}/dist/gnome/extension.js"
      dest: "{{ desktop_hyper_window_tiling_gnome_dir }}/extension.js"
  loop_control:
    label: "{{ item.dest | basename }}"
  when: desktop_hyper_has_gnome
  tags:
    - host
    - desktop
    - hyper-window-tiling

- name: Install Hyper GNOME extension schema
  ansible.builtin.copy:
    src: >-
      {{
        desktop_hyper_window_tiling_package
        ~ '/gnome/schemas/org.gnome.shell.extensions.hyper-window-tiling.gschema.xml'
      }}
    dest: >-
      {{
        desktop_hyper_window_tiling_gnome_dir
        ~ '/schemas/org.gnome.shell.extensions.hyper-window-tiling.gschema.xml'
      }}
    mode: "0644"
  register: desktop_hyper_gnome_schema
  when: desktop_hyper_has_gnome
  tags:
    - host
    - desktop
    - hyper-window-tiling

- name: Compile Hyper GNOME extension schemas
  ansible.builtin.command:
    argv:
      - glib-compile-schemas
      - "{{ desktop_hyper_window_tiling_gnome_dir }}/schemas"
  changed_when: true
  when:
    - desktop_hyper_has_gnome
    - desktop_hyper_gnome_schema is changed
  tags:
    - host
    - desktop
    - hyper-window-tiling

- name: Read enabled GNOME extensions for Hyper window tiling
  community.general.dconf:
    key: /org/gnome/shell/enabled-extensions
    state: read
  register: desktop_hyper_gnome_extensions
  when: desktop_hyper_has_gnome
  tags:
    - host
    - desktop
    - hyper-window-tiling

- name: Persist Hyper GNOME extension activation
  community.general.dconf:
    key: /org/gnome/shell/enabled-extensions
    value: >-
      {{
        (
          (
            desktop_hyper_gnome_extensions.value
            | default('@as []', true)
            | ansible.builtin.regex_replace('^@as\\s+', '')
            | ansible.builtin.from_yaml
          )
          + [desktop_hyper_window_tiling_uuid]
        )
        | ansible.builtin.unique
        | ansible.builtin.list
        | ansible.builtin.to_json
      }}
    state: present
  when: desktop_hyper_has_gnome
  tags:
    - host
    - desktop
    - hyper-window-tiling

- name: Configure Hyper GNOME extension shortcuts
  community.general.dconf:
    key: "/org/gnome/shell/extensions/hyper-window-tiling/{{ item.key }}"
    value: "{{ item.value }}"
    state: present
  loop: "{{ desktop_hyper_window_tiling_gnome_keys }}"
  loop_control:
    label: "{{ item.key }}"
  when: desktop_hyper_has_gnome
  tags:
    - host
    - desktop
    - hyper-window-tiling

- name: Free GNOME workspace navigation shortcuts for Hyper window tiling
  community.general.dconf:
    key: "/org/gnome/desktop/wm/keybindings/{{ item }}"
    value: "@as []"
    state: present
  loop:
    - switch-to-workspace-left
    - switch-to-workspace-right
    - move-to-workspace-left
    - move-to-workspace-right
  when: desktop_hyper_has_gnome
  tags:
    - host
    - desktop
    - hyper-window-tiling

- name: List enabled GNOME extensions for Hyper window tiling
  ansible.builtin.command:
    argv:
      - gnome-extensions
      - list
      - --enabled
  changed_when: false
  failed_when: false
  check_mode: false
  register: desktop_hyper_gnome_enabled
  when: desktop_hyper_has_gnome
  tags:
    - host
    - desktop
    - hyper-window-tiling

- name: Enable Hyper GNOME extension in the active session
  ansible.builtin.command:
    argv:
      - gnome-extensions
      - enable
      - "{{ desktop_hyper_window_tiling_uuid }}"
  changed_when: desktop_hyper_gnome_enable.rc == 0
  failed_when: false
  register: desktop_hyper_gnome_enable
  when:
    - not ansible_check_mode
    - desktop_hyper_has_gnome
    - >-
      desktop_hyper_window_tiling_uuid
      not in desktop_hyper_gnome_enabled.stdout_lines | default([])
  tags:
    - host
    - desktop
    - hyper-window-tiling

- name: Create Hyper KWin script directories
  ansible.builtin.file:
    path: "{{ item.0 }}/{{ item.1 }}"
    state: directory
    mode: "0755"
  loop: >-
    {{
      desktop_hyper_window_tiling_kwin_dirs
      | product(['', 'contents', 'contents/code'])
      | list
    }}
  when: desktop_hyper_has_kde
  tags:
    - host
    - desktop
    - hyper-window-tiling

- name: Install Hyper KWin scripts
  ansible.builtin.copy:
    src: "{{ item.1.src }}"
    dest: "{{ item.0 }}/{{ item.1.dest }}"
    mode: "0644"
  loop: >-
    {{
      desktop_hyper_window_tiling_kwin_dirs
      | product([
          {
            'src': desktop_hyper_window_tiling_package ~ '/kde/metadata.json',
            'dest': 'metadata.json'
          },
          {
            'src': desktop_hyper_window_tiling_package
              ~ '/dist/kde/contents/code/main.js',
            'dest': 'contents/code/main.js'
          }
        ])
      | list
    }}
  register: desktop_hyper_kwin_scripts
  when: desktop_hyper_has_kde
  tags:
    - host
    - desktop
    - hyper-window-tiling

- name: Enable Hyper KWin script
  community.general.ini_file:
    path: "{{ dotfiles_config_home }}/kwinrc"
    section: Plugins
    option: hyper-window-tilingEnabled
    value: "true"
    no_extra_spaces: true
    mode: "0600"
  register: desktop_hyper_kwin_config
  when: desktop_hyper_has_kde
  tags:
    - host
    - desktop
    - hyper-window-tiling

- name: Detect KDE D-Bus command
  ansible.builtin.command:
    argv:
      - sh
      - -c
      - command -v qdbus6 || command -v qdbus
  changed_when: false
  failed_when: false
  check_mode: false
  register: desktop_hyper_qdbus
  when: desktop_hyper_has_kde
  tags:
    - host
    - desktop
    - hyper-window-tiling

- name: Reload KWin after Hyper script changes
  ansible.builtin.command:
    argv:
      - "{{ desktop_hyper_qdbus.stdout | trim }}"
      - org.kde.KWin
      - /KWin
      - reconfigure
  changed_when: false
  failed_when: false
  when:
    - not ansible_check_mode
    - desktop_hyper_has_kde
    - desktop_hyper_qdbus.rc == 0
    - desktop_hyper_kwin_scripts is changed or desktop_hyper_kwin_config is changed
  tags:
    - host
    - desktop
    - hyper-window-tiling
