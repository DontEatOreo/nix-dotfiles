---
- name: Create Sushi Flatpak build cache
  ansible.builtin.file:
    path: "{{ desktop_sushi_cache_root }}"
    state: directory
    mode: "0755"
  when: desktop_flatpak_command.rc == 0
  tags:
    - host
    - flatpak
    - sushi-preview

- name: Check out pinned Sushi source
  ansible.builtin.git:
    repo: "{{ desktop_sushi_repository }}"
    dest: "{{ desktop_sushi_source_dir }}"
    version: "{{ desktop_sushi_revision }}"
    force: true
  register: desktop_sushi_source
  when: desktop_flatpak_command.rc == 0
  tags:
    - host
    - flatpak
    - sushi-preview

- name: Check pinned Sushi manifest
  ansible.builtin.stat:
    path: "{{ desktop_sushi_upstream_manifest }}"
  register: desktop_sushi_upstream_manifest_file
  when: desktop_flatpak_command.rc == 0
  tags:
    - host
    - flatpak
    - sushi-preview

- name: Require pinned Sushi manifest
  ansible.builtin.assert:
    that:
      - desktop_sushi_upstream_manifest_file.stat.exists | default(false)
    fail_msg: >-
      Pinned Sushi source does not contain the expected Flatpak manifest at
      {{ desktop_sushi_upstream_manifest }}.
  when:
    - desktop_flatpak_command.rc == 0
    - not (ansible_check_mode and desktop_sushi_source is changed)
  tags:
    - host
    - flatpak
    - sushi-preview

- name: Read pinned Sushi manifest
  ansible.builtin.slurp:
    src: "{{ desktop_sushi_upstream_manifest }}"
  register: desktop_sushi_manifest_source
  when:
    - desktop_flatpak_command.rc == 0
    - desktop_sushi_upstream_manifest_file.stat.exists | default(false)
  tags:
    - host
    - flatpak
    - sushi-preview

- name: Initialize pinned Sushi manifest data
  ansible.builtin.set_fact:
    desktop_sushi_manifest_data: >-
      {{ desktop_sushi_manifest_source.content | b64decode | from_json }}
    desktop_sushi_manifest_modules: []
  when: desktop_sushi_manifest_source is not skipped
  tags:
    - host
    - flatpak
    - sushi-preview

- name: Configure pinned Sushi manifest modules
  ansible.builtin.set_fact:
    desktop_sushi_manifest_modules: >-
      {{
        desktop_sushi_manifest_modules
        + [
          item
          | combine(
              {
                'config-opts': (
                  item['config-opts']
                  | default([])
                  | reject('match', '^-Dprofile=')
                  | list
                ) + ['-Dprofile=' ~ desktop_sushi_profile]
              }
              if item.name | default('') == 'sushi'
              else {}
            )
        ]
      }}
  loop: "{{ desktop_sushi_manifest_data.modules | default([]) }}"
  loop_control:
    label: "{{ item.name | default('unnamed module') }}"
  when: desktop_sushi_manifest_source is not skipped
  tags:
    - host
    - flatpak
    - sushi-preview

- name: Render pinned Sushi Flatpak manifest
  ansible.builtin.copy:
    content: >-
      {{
        desktop_sushi_manifest_data
        | combine({
            'app-id': desktop_sushi_app_id,
            'modules': desktop_sushi_manifest_modules
          })
        | to_nice_json(indent=2)
      }}
    dest: "{{ desktop_sushi_manifest }}"
    mode: "0644"
  register: desktop_sushi_rendered_manifest
  when: desktop_sushi_manifest_source is not skipped
  tags:
    - host
    - flatpak
    - sushi-preview

- name: Check installed Sushi Flatpak
  ansible.builtin.command:
    argv:
      - flatpak
      - info
      - --user
      - "{{ desktop_sushi_app_id }}"
  changed_when: false
  failed_when: false
  check_mode: false
  register: desktop_sushi_installed
  when: desktop_flatpak_command.rc == 0
  tags:
    - host
    - flatpak
    - sushi-preview

- name: Build and install pinned Sushi Flatpak
  ansible.builtin.command:
    argv:
      - flatpak
      - run
      - "--filesystem={{ desktop_sushi_cache_root }}"
      - org.flatpak.Builder
      - --user
      - --install
      - --force-clean
      - "--state-dir={{ desktop_sushi_builder_state_dir }}"
      - "{{ desktop_sushi_build_dir }}"
      - "{{ desktop_sushi_manifest }}"
  changed_when: true
  register: desktop_sushi_build
  notify:
    - Stop running Sushi Flatpak process
    - Stop Nautilus after Sushi rebuild
  when:
    - not ansible_check_mode
    - desktop_sushi_manifest_source is not skipped
    - >-
      desktop_sushi_source is changed
      or desktop_sushi_rendered_manifest is changed
      or desktop_sushi_installed.rc != 0
  tags:
    - host
    - flatpak
    - sushi-preview

- name: Read Sushi Flatpak overrides
  ansible.builtin.command:
    argv:
      - flatpak
      - override
      - --user
      - --show
      - "{{ desktop_sushi_app_id }}"
  changed_when: false
  failed_when: false
  check_mode: false
  register: desktop_sushi_overrides
  when: desktop_flatpak_command.rc == 0
  tags:
    - host
    - flatpak
    - sushi-preview

- name: Configure Sushi Flatpak graphics environment
  ansible.builtin.command:
    argv:
      - flatpak
      - override
      - --user
      - --env=GDK_GL=gles
      - "{{ desktop_sushi_app_id }}"
  changed_when: true
  when:
    - not ansible_check_mode
    - desktop_flatpak_command.rc == 0
    - desktop_sushi_installed.rc == 0 or desktop_sushi_build is changed
    - desktop_sushi_overrides.stdout is not search('(?m)^GDK_GL=gles$')
  tags:
    - host
    - flatpak
    - sushi-preview
