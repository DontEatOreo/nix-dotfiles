---
- name: Apply the managed Shottr license state
  ansible.builtin.command:
    argv:
      - "{{ platform_homebrew_prefix }}/bin/shottr-license"
      - install
  environment:
    PATH: "{{ platform_homebrew_path | default(dotfiles_path) }}"
    SOPS_AGE_KEY_CMD: >-
      {{ dotfiles_repo_root }}/dotfiles/dot_local/bin/executable_sops-age-key-1password
  register: applications_shottr_license
  changed_when: >-
    'license state installed' in applications_shottr_license.stderr
