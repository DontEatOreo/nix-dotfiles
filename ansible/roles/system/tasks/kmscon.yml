---
- name: Check KMSCON executable
  ansible.builtin.stat:
    path: /usr/bin/kmscon
  register: system_kmscon_binary

- name: Configure KMSCON when installed in the host image
  when: system_kmscon_binary.stat.executable | default(false)
  block:
    - name: Check KMSCON system prerequisites
      ansible.builtin.stat:
        path: "{{ item }}"
      loop:
        - /usr/bin/python3.14
        - /usr/bin/loginctl
        - /usr/bin/systemctl
        - /usr/lib64/kmscon/mod-freetype.so
        - /usr/lib/systemd/system/kmsconvt@.service
      register: system_kmscon_prerequisites

    - name: Check KMSCON theme Python dependency
      ansible.builtin.command:
        argv:
          - /usr/bin/python3.14
          - -c
          - >-
            import sys;
            assert sys.version_info[:2] == (3, 14), sys.version;
            sys.path.insert(0, "/usr/lib/dotfiles/python");
            import astral;
            assert astral.__version__ == "3.2"
      changed_when: false
      check_mode: false
      failed_when: false
      register: system_kmscon_astral

    - name: Check KMSCON terminfo
      ansible.builtin.command:
        argv:
          - infocmp
          - kmscon
      changed_when: false
      check_mode: false
      register: system_kmscon_terminfo

    - name: Require complete KMSCON host support
      ansible.builtin.assert:
        that:
          - >-
            system_kmscon_prerequisites.results
            | map(attribute='stat.exists')
            | select('equalto', false)
            | list
            | length == 0
          - system_kmscon_terminfo.rc == 0
          - system_kmscon_astral.rc == 0
        fail_msg: >-
          KMSCON is installed, but its FreeType renderer, Python 3.14/Astral 3.2
          runtime, systemd tools, service template, or terminfo entry is missing
          from the host image.

    - name: Create KMSCON configuration directories
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
        mode: "0755"
      become: true
      loop:
        - /etc/default
        - /etc/kmscon
        - /etc/systemd/system
        - /etc/systemd/system/kmsconvt@.service.d
        - /usr/local/libexec/dotfiles
        - /usr/local/share/dotfiles

    - name: Install KMSCON helper programs and palette
      ansible.builtin.copy:
        src: "{{ item.src }}"
        dest: "{{ item.dest }}"
        mode: "{{ item.mode }}"
      become: true
      loop:
        - src: kmscon/kmscon-theme-config.py
          dest: /usr/local/libexec/dotfiles/kmscon-theme-config
          mode: "0755"
        - src: kmscon/kmscon-refresh.py
          dest: /usr/local/libexec/dotfiles/kmscon-refresh
          mode: "0755"
        - src: "{{ dotfiles_repo_root }}/dotfiles/.chezmoitemplates/black_rose_doll_palette.json"
          dest: /usr/local/share/dotfiles/black_rose_doll_palette.json
          mode: "0644"
      loop_control:
        label: "{{ item.dest }}"

    - name: Install editable KMSCON theme defaults
      ansible.builtin.copy:
        dest: /etc/default/kmscon-dotfiles
        mode: "0644"
        force: false
        content: |
          # Sofia center is the default location.
          # DOTFILES_KMSCON_LATITUDE=
          # DOTFILES_KMSCON_LONGITUDE=
          # DOTFILES_KMSCON_THEME=light
          # DOTFILES_KMSCON_THEME=dark
      become: true

    - name: Install KMSCON systemd units
      ansible.builtin.copy:
        dest: "{{ item.dest }}"
        mode: "0644"
        content: "{{ item.content }}"
      become: true
      loop:
        - dest: /etc/systemd/system/kmsconvt@.service.d/10-dotfiles-theme.conf
          content: |
            [Service]
            EnvironmentFile=-/etc/default/kmscon-dotfiles
            ExecStartPre=/usr/bin/python3.14 /usr/local/libexec/dotfiles/kmscon-theme-config /usr/local/share/dotfiles/black_rose_doll_palette.json /etc/kmscon/kmscon.conf
        - dest: /etc/systemd/system/kmscon-theme-refresh.service
          content: |
            [Unit]
            Description=Refresh KMSCON theme
            Documentation=https://github.com/kmscon/kmscon

            [Service]
            Type=oneshot
            EnvironmentFile=-/etc/default/kmscon-dotfiles
            ExecStart=/usr/bin/python3.14 /usr/local/libexec/dotfiles/kmscon-theme-config /usr/local/share/dotfiles/black_rose_doll_palette.json /etc/kmscon/kmscon.conf
            ExecStartPost=/usr/bin/python3.14 /usr/local/libexec/dotfiles/kmscon-refresh /etc/kmscon/kmscon.conf
        - dest: /etc/systemd/system/kmscon-theme-refresh.timer
          content: |
            [Unit]
            Description=Refresh KMSCON theme around day/night changes

            [Timer]
            OnBootSec=2m
            OnUnitActiveSec=5m
            AccuracySec=30s

            [Install]
            WantedBy=timers.target
      loop_control:
        label: "{{ item.dest }}"

    - name: Use KMSCON for automatic virtual terminals
      ansible.builtin.file:
        src: /usr/lib/systemd/system/kmsconvt@.service
        dest: /etc/systemd/system/autovt@.service
        state: link
        force: true
      become: true

    - name: Remove the tty1 getty activation link
      ansible.builtin.file:
        path: /etc/systemd/system/getty.target.wants/getty@tty1.service
        state: absent
      become: true

    - name: Activate KMSCON on tty1
      ansible.builtin.file:
        src: /usr/lib/systemd/system/kmsconvt@.service
        dest: /etc/systemd/system/getty.target.wants/kmsconvt@tty1.service
        state: link
        force: true
      become: true

    - name: Enable KMSCON theme refresh timer
      ansible.builtin.systemd_service:
        name: kmscon-theme-refresh.timer
        enabled: true
        state: started
        daemon_reload: true
      become: true

    - name: Render the current KMSCON theme
      ansible.builtin.systemd_service:
        name: kmscon-theme-refresh.service
        state: started
      become: true
      changed_when: false
